import './styles.css';       // Import global styles for the app
import initialLoad from './pageLoad';  // Import the function that sets up the initial page content

initialLoad();  // Run the initial load to setup tabs and display the home page content
